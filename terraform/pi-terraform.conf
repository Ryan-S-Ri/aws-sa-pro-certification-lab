# Terraform configuration optimized for Raspberry Pi

# Reduce parallelism to avoid overwhelming Pi CPU/memory
parallelism = 2

# Plugin cache to avoid re-downloading
plugin_cache_dir = "/home/ryan/.terraform.d/plugin-cache"
